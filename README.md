# Treelinux Sprout Modules

Modules for the Treelinux sprout package manager.

## Structure
- `software/` - Application and software modules
- `drivers/` - Hardware driver modules (empty for now)

## Usage
```
sprout modules    # Download/update modules
sprout run <module.smp>    # Run a module
```
