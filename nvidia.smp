#!/usr/bin/env micropython
import sprout

# NVIDIA driver installation
gpu = sprout.detect("gpu")
if gpu == "nvidia":
    sprout.info("NVIDIA GPU detected, installing drivers...")
    sprout.apply("nvidia")
    sprout.service_enable("nvidia-persistenced")
else:
    sprout.info(f"No NVIDIA GPU detected (found: {gpu})")
